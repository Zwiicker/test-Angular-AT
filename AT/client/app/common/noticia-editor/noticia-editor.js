import angular from 'angular';
import noticiaEditorComponent from './noticia-editor.component';

let noticiaEditorModule = angular.module('noticiaEditor', [

])

.component('noticiaEditor', noticiaEditorComponent)

.name;

export default noticiaEditorModule;
