import angular from 'angular';
import noticiaDetailComponent from './noticia-detail.component';

let noticiaDetailModule = angular.module('noticiaDetail', [

])

.component('noticiaDetail', noticiaDetailComponent)

.name;

export default noticiaDetailModule;
